import assert from 'node:assert/strict';import {CATALOG} from './catalog.js';import {productToEntry,physicalParts} from './inventory.js';import {generateCombinations,isLegalDeck} from './combinations.js';import {analyze,chooseTest} from './coach.js';
const codes=['UX-14','CX-16','UX-19','UX-08','CX-09','CX-10','BX-23','UX-11','BX-49'];const inv=codes.map(code=>productToEntry(CATALOG.find(p=>p.code===code)));const combos=generateCombinations(physicalParts(inv));assert(combos.length>1000);assert(!combos.some(c=>c.parts.some(p=>p.name==='BulletGriffon')&&c.parts.some(p=>p.category==='ratchet')));const result=analyze(inv,[]);assert(result.ready);assert.equal(result.decks[0].combos.length,3);assert(isLegalDeck(result.decks[0].combos));const ratchets=result.decks[0].combos.flatMap(c=>c.parts.filter(p=>p.category==='ratchet').map(p=>p.name));assert.equal(ratchets.filter(x=>x==='9-60').length<=1,true);const test=chooseTest(result.decks[0],result.combos,[]);assert(test?.opponent?.name);console.log(JSON.stringify({catalog:CATALOG.length,combos:combos.length,decks:result.evaluated,test:`${test.subject.name} vs ${test.opponent.name}`},null,2));
