{
  "name": "x-command-center",
  "version": "1.1.0",
  "private": true,
  "description": "Static Beyblade X tournament command center",
  "scripts": {
    "test": "node test.mjs",
    "check": "node --check data.js && node --check core.js && node --check app.js && node --check service-worker.js && node test.mjs"
  },
  "engines": { "node": ">=20" }
}
